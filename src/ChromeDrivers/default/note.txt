This directory contains chromedriver version 100.0.4896.60 for a few different OS.

It is used as a default fixed path so Chrome versions can be checked and other versions can be downloaded accordingly.